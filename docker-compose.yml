version: '3.3'

services:
  tps: #topic predictor service
    build: ./src/tps
    working_dir: /tps
    volumes:
      - ./src/tps:/tps
    ports:
      - ${TPS_PORT}:${TPS_PORT}
    networks:
      - localnet

networks:
  localnet:
    driver: bridge
